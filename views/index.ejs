<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>File uploader</title>
    <style>
        #file-previews {
            display: none;
            width: 20%;
            height: 40%;
        }
    </style>
</head>
<body>
    <header>
        Hello
    </header>
    <main>
        <p>Image uploader</p>
        <form method="post" action="upload/post" enctype="multipart/form-data">
            <input type="file" name="image" id="file-ip-1" onchange="showPreview(event);"/><br /><br />
            <button type="submit" name="upload">Upload</button>
          </form>
        <p>Preview</p>
        <div class="preview">
            <img id ="file-previews">
        </div>
    </main>
    <script>
        function showPreview(event) {
    if(event.target.files.length > 0) {
        let src = URL.createObjectURL(event.target.files[0]);
        let preview = document.getElementById("file-previews");
        preview.src = src;
        preview.style.display = "block"
    }
}
    </script>
</body>
</html>